# A generic, single database configuration for ClickHouse migrations.
[alembic]
databases = local
version_path_separator = os  # Use os.pathsep. Default configuration used for new projects.

[DEFAULT]
# path to migration scripts
script_location = .
prepend_sys_path = .

# template used to generate migration file names; The default value is %%(rev)s_%%(slug)s
# Uncomment the line below if you want the files to be prepended with date and time
# see https://alembic.sqlalchemy.org/en/latest/tutorial.html#editing-the-ini-file
# for all available tokens
file_template = %%(year)d_%%(month).2d_%%(day).2d_%%(hour).2d%%(minute).2d_%%(slug)s

# sys.path path, will be prepended to sys.path if present.
# defaults to the current working directory.

# timezone to use when rendering the date within the migration file
# as well as the filename.
# If specified, requires the python-dateutil library that can be
# installed by adding `alembic[tz]` to the pip requirements
# string value is passed to dateutil.tz.gettz()
# leave blank for localtime
# timezone =

# max length of characters to apply to the
# "slug" field
# truncate_slug_length = 40

# set to 'true' to run the environment during
# the 'revision' command, regardless of autogenerate
# revision_environment = false

# set to 'true' to allow .pyc and .pyo files without
# a source .py file to be detected as revisions in the
# versions/ directory
# sourceless = false

# version location specification; This defaults
# to migrations/versions.  When using multiple version
# directories, initial revisions must be specified with --version-path.
# The path separator used here should be the separator specified by "version_path_separator" below.
# version_locations = %(here)s/bar:%(here)s/bat:migrations/versions

# version path separator; As mentioned above, this is the character used to split
# version_locations. The default within new alembic.ini files is "os", which uses os.pathsep.
# If this key is omitted entirely, it falls back to the legacy behavior of splitting on spaces and/or commas.
# Valid values for version_path_separator are:
#
# version_path_separator = :
# version_path_separator = ;
# version_path_separator = space

# set to 'true' to search source files recursively
# in each "version_locations" directory
# new in Alembic version 1.10
# recursive_version_locations = false

# the output encoding used when revision files
# are written from script.py.mako
# output_encoding = utf-8


# ENVIRONMENT CONFIGURATIONS
# Configure your database environments here
# Example configurations are provided below - customize as needed

# Local development environment
[local]
sqlalchemy.url = clickhouse+http://%(LOCAL_USERNAME)s:%(LOCAL_PASSWORD)s@%(LOCAL_HOST)s:%(LOCAL_PORT)s/%(LOCAL_DATABASE)s%(LOCAL_PROTOCOL_PARAMS)s
version_locations = clickhouse_migrations

# Development environment (uncomment and configure as needed)
# [dev]
# sqlalchemy.url = clickhouse+http://%(DEV_USERNAME)s:%(DEV_PASSWORD)s@%(DEV_HOST)s:%(DEV_PORT)s/%(DEV_DATABASE)s%(DEV_PROTOCOL_PARAMS)s
# version_locations = clickhouse_migrations

# Staging environment (uncomment and configure as needed)  
# [staging]
# sqlalchemy.url = clickhouse+http://%(STAGING_USERNAME)s:%(STAGING_PASSWORD)s@%(STAGING_HOST)s:%(STAGING_PORT)s/%(STAGING_DATABASE)s%(STAGING_PROTOCOL_PARAMS)s
# version_locations = clickhouse_migrations

# Production environment (uncomment and configure as needed)
# [prod]
# sqlalchemy.url = clickhouse+http://%(PROD_USERNAME)s:%(PROD_PASSWORD)s@%(PROD_HOST)s:%(PROD_PORT)s/%(PROD_DATABASE)s%(PROD_PROTOCOL_PARAMS)s
# version_locations = clickhouse_migrations

[post_write_hooks]
# post_write_hooks defines scripts or Python functions that are run
# on newly generated revision scripts.  See the documentation for further
# detail and examples

# format using "black" - use the console_scripts runner, against the "black" entrypoint
hooks = black
black.type = console_scripts
black.entrypoint = black
; black.options = -l 79 REVISION_SCRIPT_FILENAME

# Logging configuration
[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S